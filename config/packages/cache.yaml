framework:
    cache:
        # Put the unique name of your assets here: the prefix seed
        # is used to compute stable namespaces for cache keys.
        #prefix_seed: your_vendor_name/app_name

        # The assets cache caches to the filesystem by default.
        # Other options include:

        # Redis
        #assets: cache.adapter.redis
        #default_redis_provider: redis://localhost

        # APCu (not recommended with heavy random-write workloads as memory fragmentation can cause perf issues)
        #assets: cache.adapter.apcu

        # Namespaced pools use the above "assets" backend by default
        #pools:
            #my.dedicated.cache: ~
